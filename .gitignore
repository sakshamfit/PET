node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.production.example

# Desktop packaging output
release/
dist_electron/

# Control-plane server runtime data (never commit real databases or backups)
server/data/
server/backups/
server/uploads/
server/logs/
server/public/admin/
server/*.db
server/*.db-wal
server/*.db-shm

# Electron build caches
out/

# Server logs (service stdout/stderr on the office PC)
logs/

# Desktop build-time config (written by write-build-config.mjs from env)
electron/build-config.json

# ---- Android (Capacitor) ----
android/app/build/
android/app/release/
android/.gradle/
android/build/
android/.idea/
android/local.properties
# PET Ops shell (android-pet/). Same generated outputs, different project.
android-pet/app/build/
android-pet/app/release/
android-pet/.gradle/
android-pet/build/
android-pet/.idea/
android-pet/local.properties
android-pet/capacitor-cordova-android-plugins/
android-pet/app/src/main/assets/
*.keystore
*.jks
SchoolManagement-Android*.apk
PET-Ops*.apk
local.properties
# Regenerated from dist/ by `npx cap sync android` on every build
android/app/src/main/assets/public/
server/public/app/
